spring:
  config:
    activate:
      on-profile: local
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/donmit?autoReconnect=true&useUnicode=true
    username: root
    password: xptmxm
